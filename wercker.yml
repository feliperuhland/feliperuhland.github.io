box: debian
build:
  steps:
    - script:
        name: install git
        code: |
          apt-get update && apt-get install git -y
    - arjen/hugo-build:
        version: "0.15"
        theme: allegiant
deploy:
  steps:
    - script:
        name: install git
        code: |
          apt-get update && apt-get install git -y
